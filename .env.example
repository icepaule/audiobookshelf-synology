# =============================================================================
# Audiobookshelf - Umgebungsvariablen (Vorlage)
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
# =============================================================================

# --- Audiobookshelf Server ---
ABS_PORT=13378
ABS_CONTAINER_NAME=audiobookshelf
ABS_IMAGE=ghcr.io/advplyr/audiobookshelf:latest
TZ=Europe/Berlin

# --- Pfade auf dem Host ---
ABS_CONFIG_PATH=<pfad-zum-projekt>/config
ABS_METADATA_PATH=<pfad-zum-projekt>/metadata
ABS_AUDIOBOOKS_PATH=<pfad-zu-eigenen-hoerbuechern>

# --- Bestehende Sammlungen (separate Bibliotheken, read-only) ---
ABS_MOUNT_NAS=<pfad-zur-nas-sammlung>
ABS_MOUNT_USB=<pfad-zur-usb-sammlung>

# --- Audiobookshelf API ---
ABS_URL=http://localhost:13378
ABS_API_TOKEN=

# --- Ollama ---
OLLAMA_URL=http://<ollama-host>:11434
OLLAMA_MODEL=qwen2.5:14b
